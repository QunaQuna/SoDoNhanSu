@model IEnumerable<_2.Models.Employee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.emp_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.emp_sex)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.emp_position)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.job_position)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.supervisor_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Company.company_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.emp_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.emp_sex)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.emp_position)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.job_position)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.supervisor_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Company.company_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.emp_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.emp_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.emp_id })
        </td>
    </tr>
}

</table>
